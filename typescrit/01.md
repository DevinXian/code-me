### infer

```typescript
// 判断方法返回值
type ReturnType<T> = T extends (...args: any[]) => infer R ? R : any;
type ReturnPromiseString = ReturnType<(n: number) => Promise<string>>

// 解包 Promise
type unpackPromise<T> = T extends Promise<infer R> ? R : any;
```